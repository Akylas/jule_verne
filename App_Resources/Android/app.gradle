
android { 

  defaultConfig {  
    generatedDensities = []
    compileSdkVersion 29
    multiDexEnabled true
    minSdkVersion 17
    targetSdkVersion 29
    versionCode 1
    versionName "1.0.0"
    ndk {
      abiFilters.clear()
    }
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"  
  }

}

